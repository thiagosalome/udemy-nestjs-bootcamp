import { Injectable } from '@nestjs/common';
@Injectable()
export class AppService {}

/*

Options to configure Postgres

  1 - Install Postgres on local machine (the installation is more complicated)
  2 - Host Postgres in cloud (easiest option)

  Heroku doesn't have a free plan anymore. Because of that a suggestion is use Docker.
*/
